seed: 42

data:
  edge_dir: data/raw/edge
  rgb_dir: data/raw/rgb
  image_size: 256

model:
  channels: 64

train:
  batch_size: 8
  lr: 0.0002
  epochs: 100
  lambda_l1: 100.0
  sample_every: 200
  checkpoint_every: 500

paths:
  sample_dir: outputs/samples/stage1_pix2pix
  checkpoint_dir: checkpoints/stage1/pix2pix
